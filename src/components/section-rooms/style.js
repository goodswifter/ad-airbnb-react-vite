import styled from 'styled-components'

export const RoomsWrapper = styled.div`
  background-color: #f5f5f5;
  display: flex;
  flex-wrap: wrap;
  /* 为容器添加左右 8px 的内边距 */
  padding: 8px;
  border-radius: 8px;
  margin: 0 -8px;
`
